import { Text, TextInput, View } from 'react-native';
import { router } from 'expo-router';
import { AuthScreen, authStyles } from '@/components/AuthScreen'; import { Button } from '@/components/Button'; import { useTheme } from '@/providers/ThemeProvider';
export default function Register() { const theme = useTheme(); const field = (label: string, password = false) => <View style={authStyles.field}><Text style={[authStyles.label, { color: theme.text }]}>{label}</Text><TextInput accessibilityLabel={label} secureTextEntry={password} autoCapitalize="none" style={[authStyles.input, { color: theme.text, borderColor: theme.border }]} /></View>; return <AuthScreen title="Create your account" subtitle="Start with your schedule, then let CampusLife Buddy keep you on track.">{field('Full name')}{field('Email')}{field('Password', true)}{field('Confirm password', true)}<Button label="Create account" onPress={() => router.replace('/(tabs)')} /><Text style={[authStyles.helper, { color: theme.secondary }]}>By continuing, you confirm this app does not access your official Campus Life account.</Text><Text onPress={() => router.replace('/(auth)/login')} style={[authStyles.link, { color: theme.text }]}>Already have an account? Sign in</Text></AuthScreen>; }
