import { Text, TextInput, View } from 'react-native'; import { router } from 'expo-router'; import { AuthScreen, authStyles } from '@/components/AuthScreen'; import { Button } from '@/components/Button'; import { useTheme } from '@/providers/ThemeProvider';
export default function ForgotPassword() { const theme = useTheme(); return <AuthScreen title="Reset your password" subtitle="Enter your email and we’ll send reset instructions when the backend is connected."><View style={authStyles.field}><Text style={[authStyles.label, { color: theme.text }]}>Email</Text><TextInput accessibilityLabel="Email" autoCapitalize="none" keyboardType="email-address" style={[authStyles.input, { color: theme.text, borderColor: theme.border }]} /></View><Button label="Send reset instructions" onPress={() => router.back()} /><Text onPress={() => router.back()} style={[authStyles.link, { color: theme.text }]}>Back to sign in</Text></AuthScreen>; }
